import DashboardLayout from '../components/DashboardLayout';

export const metadata = {
  title: 'AI 이미지 만들기 — ToonCraft',
};

export default function AiImageGeneratorPage() {
  return <DashboardLayout />;
}
